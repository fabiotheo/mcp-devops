/**
 * Constants used throughout the MCP Ink CLI application
 */

// Marker for cancelled messages in history
export const CANCELLATION_MARKER =
  '[A mensagem anterior foi cancelada pelo usuário com ESC antes de ser respondida]';

export default {
  CANCELLATION_MARKER
};